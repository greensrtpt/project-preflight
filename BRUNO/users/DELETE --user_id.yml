info:
  name: DELETE /:user_id
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/users/{{user_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
