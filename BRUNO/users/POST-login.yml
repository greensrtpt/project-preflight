info:
  name: POST/login
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/users/login"
  body:
    type: json
    data: |-
      {
        "username": "green_test",
        "password": "123456789"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
