info:
  name: GET
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/users"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
