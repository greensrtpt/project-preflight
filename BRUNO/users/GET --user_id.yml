info:
  name: GET /:user_id
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/users/:user_id"
  params:
    - name: user_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
