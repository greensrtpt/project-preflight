info:
  name: GET /:user_id
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/users/{{user_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
