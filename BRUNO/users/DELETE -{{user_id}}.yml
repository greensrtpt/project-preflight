info:
  name: DELETE /{{user_id}}
  type: http
  seq: 6

http:
  method: GET
  url: |-
    DELETE
    {{base_url}}/users/{{user_id}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
