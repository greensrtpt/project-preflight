info:
  name: POST /users
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:3001/users
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
