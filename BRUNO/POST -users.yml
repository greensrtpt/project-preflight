info:
  name: POST /users
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:3001/users
  body:
    type: json
    data: |-
      {
        "username": "green_test",
        "password": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
