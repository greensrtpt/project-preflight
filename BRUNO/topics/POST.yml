info:
  name: "POST "
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/topics"
  body:
    type: json
    data: |-
      {
        "topic_name": "กิจกรรม"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
