info:
  name: "POST "
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/topics"
  body:
    type: json
    data: |-
      {
        "topic_name": "university life"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
