info:
  name: GET /topic_id
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/topics/{{topic_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
