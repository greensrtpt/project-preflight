info:
  name: "{{base_url}}/users/{{user_id}}"
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base_url}}/users/{{user_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
