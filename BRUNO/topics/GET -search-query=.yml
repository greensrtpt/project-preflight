info:
  name: GET /search?query=
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/topics/search?query=การเรียน"
  params:
    - name: query
      value: การเรียน
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
