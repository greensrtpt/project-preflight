info:
  name: DELETE /:topic_id (Forbidden - 403)
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/topics/{{topic_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
