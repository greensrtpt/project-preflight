info:
  name: POST topic
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/topics"
  body:
    type: json
    data: |-
      {
        "topic_name": "test topic"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
