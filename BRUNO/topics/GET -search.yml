info:
  name: GET /search
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/topics/search"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
