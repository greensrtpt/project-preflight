info:
  name: GET /topics/search
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:3001/topics/search?query=การเรียน
  params:
    - name: query
      value: การเรียน
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
