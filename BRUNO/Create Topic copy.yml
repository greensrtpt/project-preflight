info:
  name: Create Topic copy
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/topics"
  headers:
    - name: Content-Type
      value: " application/json"
      disabled: true
  body:
    type: json
    data: |-
      {
        "topic_name": "การเรียน"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
