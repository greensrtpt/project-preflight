info:
  name: Create Topic
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/topics"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
