info:
  name: GET /topics/all
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/topics/all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
