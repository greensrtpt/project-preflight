info:
  name: POST/users/login
  type: http
  seq: 8

http:
  method: POST
  url: " http://localhost:3001/users/login"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
