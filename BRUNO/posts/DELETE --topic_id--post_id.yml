info:
  name: DELETE /:topic_id/:post_id
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/posts/{{topic_id}}/{{post_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
