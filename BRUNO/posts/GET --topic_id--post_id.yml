info:
  name: GET /:topic_id/:post_id
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/posts/{{topic_id}}/{{post_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
