info:
  name: POST post
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/posts/{{topic_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
