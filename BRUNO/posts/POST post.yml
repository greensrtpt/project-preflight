info:
  name: POST post
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/posts/{{group_id}}"
  body:
    type: json
    data: |-
      {
        "title": "Test Post",
        "descriptions": "Test post in Board Game"
      }
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
