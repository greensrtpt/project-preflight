info:
  name: POST post
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/posts/post"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
