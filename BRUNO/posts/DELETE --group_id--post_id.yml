info:
  name: DELETE /:group_id/:post_id
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/posts/{{group_id}}/{{post_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
