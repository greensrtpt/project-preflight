info:
  name: DELETE /:group_id/:post_id
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/posts/{{group_id}}/{{post_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
