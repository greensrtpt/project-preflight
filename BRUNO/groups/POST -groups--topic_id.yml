info:
  name: POST /groups/:topic_id
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
