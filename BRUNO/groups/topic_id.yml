info:
  name: /:topic_id
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/groups/{{topic_id}}"
  body:
    type: json
    data: |-
      {
          "group_name": "Computer Science"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
