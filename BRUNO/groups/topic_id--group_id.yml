info:
  name: /:topic_id/:group_id
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/groups/{{topic_id}}/{{group_comSci}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
