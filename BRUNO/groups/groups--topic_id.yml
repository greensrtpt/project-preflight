info:
  name: " /groups/:topic_id"
  type: http
  seq: 1

http:
  method: GET
  url: " /groups/:topic_id"
  params:
    - name: topic_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
