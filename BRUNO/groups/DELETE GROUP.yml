info:
  name: ⚠️ DISABLED - DELETE GROUP (EXPECT 403)
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/groups/{{topic_id}}/{{group_comSci}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
